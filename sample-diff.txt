diff --git a/src/auth/login.ts b/src/auth/login.ts
index 8c2d4a1..a1b9f3e 100644
--- a/src/auth/login.ts
+++ b/src/auth/login.ts
@@ -42,7 +42,12 @@ export async function login(email: string, password: string) {
     throw new InvalidCredentialsError();
   }

-  return { token: generateToken(user) };
+  if (user.mfaEnabled) {
+    const challenge = await createMfaChallenge(user.id);
+    return { mfaRequired: true, challengeId: challenge.id };
+  }
+
+  return { token: generateToken(user), mfaRequired: false };
 }
